| .container h2 b | Profiles: div = link_to "Reload Profile",load_profile_home_index_path,:class=>"btn btn-info" br div | Enable or disable your daily summaries, set alerting thresholds or configure your Stack channels here. br = text_field_tag :search,nil,:placeholder=>"Search..." hr - @profiles.each do |profile| .row h3 | All Web Site Data .col-md-4 div = link_to "Save Profile",get_profile_metrics_home_index_path(:save=>"save"),:class=>"btn btn-primary btn-lg",:method=>"post" br div = button_tag"Send Daily Summary",:onclick=>"send_summary('daily', #{profile.id})",:class=>"btn btn-primary" br div = button_tag"Send Weekly Summary",:onclick=>"send_summary('weekly', #{profile.id})",:class=>"btn btn-primary" br = form_tag(get_profile_metrics_home_index_path(profile.id)) do .col-md-4 h4 b | Summaries =check_box_tag "daily" |  Enable Daily Summary br =check_box_tag "weekly" |  Enable Weekly Summary br =check_box_tag "sources" |  Send Top Sources br =check_box_tag "landing_pages" |  Send Top Landing Pages br h4 | Summary Metrics =check_box_tag "users" |  Users br =check_box_tag "new_users" |  New Users br =check_box_tag "conversions" |  Conversions br =check_box_tag "views" |  Page Views br =check_box_tag "avg_session" |  Average Session Duration br =check_box_tag "events" |  Events br =check_box_tag "rate" |  Conversion Rate br =check_box_tag "sessions" |  Sessions br .col-md-4 h4 b | Realtime Alerts h4 | Realtime Alert Metric .col-md-12 =submit_tag "Continue",:class=>"btn btn-success pull-right" 
